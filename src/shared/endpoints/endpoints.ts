export enum GlobalEndpoints {
  BASE_URL = 'https://',
  HOST = 'microsoft-translator-text.p.rapidapi.com',
}

export enum TranslatePageEndpoints {
  LANGUAGES = `api.cognitive.microsofttranslator.com/languages`,
  TRANSLATE = `microsoft-translator-text.p.rapidapi.com/translate`,
}
